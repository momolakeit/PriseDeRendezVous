config:
  securite:
    active: false
  styles:
    images:
      path: "http://localhost:8081/stylesImages"

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 15MB
